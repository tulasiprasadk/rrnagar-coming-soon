# rrnagar-coming-soon

A fresh start for the rrnagar.com website.

## Getting Started

This repository has been reset to provide a clean starting point.

## Deployment

Use the `deploy.sh` script to deploy to a remote server:

```bash
./deploy.sh --host user@server --remote-path /var/www/site
```

See `./deploy.sh --help` for all options.
